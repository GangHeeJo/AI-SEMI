# Bio-mimic AER 통신 회로 설계 — 발표 자료

## 1. 문제 정의

AER(Address-Event Representation, 주소-이벤트 표현)는 신경계의 통신 원리를 회로로 옮긴 방식이다. Mahowald(1992)가 정립한 핵심 원칙은 **"주소 자체가 이벤트다"** — 스파이크는 크기(magnitude) 정보를 담지 않고, 어떤 뉴런이 언제 발화했는지(신원+시각)만 전달한다.

본 프로젝트는 4×4(16개) 이벤트 소스를 하나의 공유 통신 채널로 중재해서 내보내는 AER 송수신 회로를 설계한다. 목표는 다음 세 축을 동시에 개선하는 것:

- **손실률**: 여러 소스가 동시에 이벤트를 발생시킬 때 채널 용량 부족으로 인한 이벤트 유실을 줄인다.
- **공정성**: 특정 소스가 영구적으로 서비스받지 못하는 상황(기아, starvation)을 없앤다.
- **실제 물리 성능(PPA)**: Genus 합성 추정치가 아니라 Place & Route까지 완주한 실측 Fmax/면적/전력으로 검증한다.

## 2. 설계 진화 — 생체모방 아이디어를 단계적으로 적용

### 2-1. 기반 구조: 행-열 2단계 계층 중재

Mahowald(1992)·Boahen(2000)이 기술한 원조 AER 방식대로, 16개 소스를 4×4 격자로 보고 **행을 먼저 중재한 뒤 그 행 안에서 열을 중재**하는 2단계 구조를 채택했다. Boahen(2000)이 제시한 "중재기 개수가 N개에서 √N개로 줄어든다"는 면적 절감 효과를 그대로 반영해, 열 중재기를 4개 행이 공유하도록 설계했다.

### 2-2. 망막의 중심와(fovea) 모방 — 공간적 우선순위

망막이 중심와 영역에 더 높은 해상도/우선순위를 부여하듯, 4×4 격자의 중심 2행에 가중치를 부여하는 가중 라운드로빈 중재를 추가했다(중심:주변 = 5:1). 중심/주변을 완전히 독립된 중재기로 분리해 상태 공유로 인한 부작용을 원천 차단했다.

### 2-3. 이벤트 클러스터링 — 열 중재기 제거

같은 행에서 여러 열이 동시에 대기 중인 상황(국소적으로 뭉친 이벤트, 예: 움직이는 물체)을 "한 열만 골라 보내는" 대신, **행이 이기면 그 행의 열 요청 4비트를 비트맵으로 통째로 전송**하도록 바꿨다. 한 번의 grant로 최대 4개 이벤트를 동시에 내보낼 수 있어, 단순히 우선순위를 재분배하는 게 아니라 **채널의 실질 용량 자체를 늘렸다**. 열 중재기가 사라져 하드웨어도 오히려 가벼워졌다.

**부수 효과**: 완전포화 상태에서 특정 소스가 영구적으로 서비스받지 못하는 기아 현상이 기존 구조(공유 열 중재기)에는 존재했는데, 열 중재기 자체가 없는 클러스터링 구조는 이 문제에 **구조적으로 면역**임을 확인했다.

### 2-4. 전용 채널 — 중심/주변 경쟁 자체를 제거

fovea의 가중치는 "중심과 주변이 같은 출력 슬롯 하나를 놓고 경쟁"할 때만 필요한 장치였다. 중심팀과 주변팀에 **각자 전용 레인**을 주어 애초에 경쟁을 없앴다 — 중심 행 하나(열 비트맵) + 주변 행 하나(열 비트맵), 최대 2행×4열=8개 이벤트/사이클.

이 단계에서 fovea 개념의 성격이 실제로 바뀐다는 점은 짚어둘 필요가 있다. fovea의 핵심은 "공간 구분"과 "제한된 자원을 중심에 유리하게 배분"이라는 두 요소로 이뤄져 있었는데, 전용 채널화는 앞의 요소(공간 구분)만 남기고 뒤의 요소(배분)는 없앤다 — 경쟁이 사라지니 배분이라는 개념 자체가 성립하지 않기 때문이다. 즉 이 지점부터는 "생물학적 중심와 모방"이 아니라 "공간별 채널 분리"라는 다른 문제를 푸는 것에 가깝다. 다만 그 전환의 실익은 정량적으로 명확하다(qualified P&R 기준):

| 항목 | fovea | cluster2 | 차이 |
|---|---:|---:|---:|
| Fmax | 714.3~769.2 MHz | 1052.6~1111.1 MHz | +40~47% |
| 면적 | 385.4 um² | 289.0 um² | -25% |
| 전력 | 73.0 uW | 77.3 uW | +5.9% |
| 손실률(15% 부하 기준) | 57.6% | 0.0% | 완전 해소 |
| 핀 수 | 5 | 14 | +9 |

### 2-5. Work-conserving 레인 공유

전용 채널은 한쪽 팀이 조용할 때 그 레인이 그냥 노는 비효율이 있다. 중심 또는 주변 중 한쪽이 완전히 비어 있으면, 그 레인을 상대 팀에게 빌려주어 **경쟁 없이 자기 행 2개를 동시에** 내보낼 수 있도록 확장했다. 4×4 토폴로지에서 "팀당 정확히 행 2개"라는 구조적 특성 덕분에, 레인을 빌리는 것이 곧 상대 팀의 두 행 각각에 전용 레인을 주는 것과 동일해지는 우아한 성질을 갖는다.

### 2-6. 불응기(refractory) 모방 — 소스별 최소 버퍼링

생물 뉴런의 시냅스처럼, 같은 소스가 서비스되기 전에 또 발화하는 경우(빠른 재발화)에 대응하기 위해 **소스별 2-deep 포화 카운터**를 도입했다. Payload가 없는 시스템이므로 이벤트끼리 값으로 구분할 필요가 없어, 진짜 FIFO(값 저장)가 아니라 "몇 개 밀려있는지"만 세는 가벼운 카운터로 충분했다 — 소스 인덱스가 곧 저장 위치이므로 별도의 주소 비교 로직이 필요 없는 구조.

### 2-7. 두 손실 축의 결합

앞선 개선들은 서로 다른 두 가지 손실 원인을 겨냥한다: **서로 다른 소스 간의 경쟁**(2-4, 2-5가 해결)과 **같은 소스의 빠른 재발화**(2-6이 해결)는 독립적인 문제이며, 두 해법을 결합해 **두 손실 축을 동시에 0%**로 만든 최종 설계를 완성했다.

### 2-8. 최소 수신부(RX)

지금까지의 회로가 송신측(중재+인코딩)이었다면, 수신측에서 이를 다시 소스별 이벤트로 복원하는 RX를 추가해 완전한 송수신 통신 시스템을 구성했다. Payload가 없는 주소=이벤트 철학 덕분에, RX는 순수 조합논리(레지스터 없음) 만으로 구현되며 TX 대비 면적·전력 비용이 10% 미만으로 사실상 무시할 수준이다.

## 3. 검증 방법론

- **기능 검증**: 무작위 트래픽(15% 부하 등)에서 생성-배달-손실 이벤트 수 보존식이 정확히 성립하는지 대규모 시뮬레이션(수만 사이클)으로 확인.
- **로버스트니스**: 완전포화·경계조건·리셋 등 극단 상황에서 기아·팬텀 이벤트가 없는지 검증.
- **물리 검증(P&R)**: Genus 합성 추정치에 머무르지 않고, 매 목표 주기마다 재합성부터 완전한 Place & Route(배치-배선-CTS)를 완주하는 공식 방법론(per_target_resynthesis)으로 setup/hold 타이밍, DRC, antenna 위반까지 전부 확인한 **qualified Fmax**를 확보했다.

## 4. 최종 설계 요약 (물리검증 완료 후보)

| 설계 | Fmax(qualified P&R) | 면적 | 전력 | 핵심 특성 |
|---|---:|---:|---:|---|
| fovea | 714.3 ~ 769.2 MHz | 385.4 um²(P&R) | 73.0 uW | 중심 우선순위 |
| cluster | 800.0 ~ 833.3 MHz | 272.2 um²(P&R) | 58.6 uW | 열 비트맵, 손실 6.9% |
| **cluster2** | **1052.6 ~ 1111.1 MHz** | 289.0 um²(P&R) | 77.3 uW | 전용 2레인, inter-source 손실 0% |
| cluster2 + steal + buf(최종) | ≥ 333.3 MHz(qualified) | 695.3 um²(Genus) | 19.9 uW(Genus) | **두 손실 축 모두 0%**, 비대칭부하 처리량 2배 |
| + RX | (TX와 동일 대역) | +58.5 um² | +1.7 uW | 완전한 송수신 시스템 |

두 후보(순수 성능 최우선의 cluster2, 손실 0%를 우선하는 결합판) 모두 물리 검증이 끝난 상태로 보유하고 있어, 평가 기준에 따라 어느 쪽이든 즉시 제출 가능하다.

## 5. 설계 공간 탐색

성능/손실률 개선 외에도, 생체모방 아이디어의 적용 가능성을 폭넓게 정량 평가했다 — payload(데이터 탑재) 부호화, rate/interval coding(발화 빈도 부호화), 공간적 동시발화 필터, 균질화(homeostatic) 유량 제어 등 여러 대안을 실제 하드웨어로 구현·측정해 비교했다. 이 과정에서 "주소=이벤트"라는 최소 프로토콜을 유지하는 현재 설계가, 공유·중재되는 통신 채널이라는 우리 시스템의 특성상 정보량을 늘리는 여러 대안들보다 면적·전력·정확도 종합에서 우위에 있음을 확인했다.
